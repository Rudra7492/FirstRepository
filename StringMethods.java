
/**
 * Write a description of class StringMethods here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class StringMethods
{
    public static void main(String args[])
    {
        String s = "Hello Java";
        String ss = " World";
        System.out.println("The length of String is : "+s.length());
        System.out.println("String in UpperCase is : "+s.toUpperCase());
        System.out.println("String in LowerCase is : "+s.toLowerCase());
        System.out.println("Concated two Sgtring is : "+s.concat(ss));
    }
}
